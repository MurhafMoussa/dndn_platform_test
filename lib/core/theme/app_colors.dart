import 'package:flutter/material.dart';

/// Design tokens for semantic colors and brand palette.
abstract final class AppColors {
  static const Color seedColor = Colors.deepPurple;
  static const Color policeColor = Colors.deepPurple;
  static const Color accidentColor = Colors.redAccent;
  static const Color trafficColor = Colors.orange;

  static const Color lightBackground = Color(0xFFF8F9FE);
  static const Color darkBackground = Color(0xFF12131A);
}
