/// Represents the application user access role.
enum UserRole {
  /// Regular user role with map and incident reporting access.
  user,

  /// Administrator role with telemetry dashboard access.
  admin,
}
